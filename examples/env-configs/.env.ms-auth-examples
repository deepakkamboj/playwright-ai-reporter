# Microsoft Authentication Configuration Examples
# For use with playwright-ms-auth package

# ============================================
# Basic Password Authentication (Default)
# ============================================
MS_USER_EMAIL=user@company.com
MS_USER_PASSWORD=your-password
MS_SHAREPOINT_URL=https://yourcompany.sharepoint.com/

# ============================================
# Certificate Authentication with Azure Key Vault
# ============================================
# MS_USER_EMAIL=user@company.com
# MS_CREDENTIAL_TYPE=certificate
# MS_CREDENTIAL_PROVIDER=azure-keyvault
# MS_KEYVAULT_ENDPOINT=https://your-vault.vault.azure.net
# MS_KEYVAULT_SECRET_NAME=your-cert-secret
# MS_SHAREPOINT_URL=https://yourcompany.sharepoint.com/

# ============================================
# Managed Identity Authentication
# ============================================
# MS_USER_EMAIL=user@company.com
# MS_CREDENTIAL_TYPE=managed-identity
# MS_CREDENTIAL_PROVIDER=azure
# MS_SHAREPOINT_URL=https://yourcompany.sharepoint.com/

# ============================================
# Local Certificate File
# ============================================
# MS_USER_EMAIL=user@company.com
# MS_CREDENTIAL_TYPE=certificate
# MS_CREDENTIAL_PROVIDER=file
# MS_CERT_PATH=/path/to/certificate.pfx
# MS_CERT_PASSWORD=cert-password
# MS_SHAREPOINT_URL=https://yourcompany.sharepoint.com/

# ============================================
# Additional Options
# ============================================

# Debug mode (verbose logging)
# MS_AUTH_DEBUG=true

# Custom browser (chromium, firefox, webkit)
# MS_AUTH_BROWSER=chromium

# Headless mode
# MS_AUTH_HEADLESS=true

# Custom timeout (milliseconds)
# MS_AUTH_TIMEOUT=30000

# Custom storage state path
# MS_AUTH_STORAGE_PATH=.auth/custom-state.json
