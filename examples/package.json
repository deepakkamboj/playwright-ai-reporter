{
    "name": "playwright-smart-reporter-examples",
    "version": "0.0.1",
    "description": "Example tests and configurations for Playwright Smart Reporter",
    "private": true,
    "scripts": {
        "test": "playwright test",
        "test:ui": "playwright test --ui",
        "test:debug": "playwright test --debug",
        "test:report": "playwright show-report",
        "test:google": "playwright test tests/google-search.test.ts",
        "test:mixed": "playwright test tests/mixed-results.test.ts",
        "test:playwright": "playwright test tests/playwright-site.test.ts"
    },
    "dependencies": {
        "playwright-smart-reporter": "file:.."
    },
    "devDependencies": {
        "@playwright/test": "^1.51.0",
        "dotenv": "^16.3.1"
    }
}
